/// <reference path="../../typings/browser.d.ts" />

module rpComponents.bridge {
	angular.module('explorer.rockproperties.clusterinspector', [])
	
	.factory('bridgeService', ['mapService', function(mapService: any) {
		var service: any = {};
		
		
		return service;
	}]);
}